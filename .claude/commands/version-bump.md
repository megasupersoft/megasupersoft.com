Bump the version in package.json. Argument should be "patch", "minor", or "major": $ARGUMENTS

If no argument given, default to "patch". Update the version field in package.json, then show the old and new version. Do NOT commit automatically — let the user decide.